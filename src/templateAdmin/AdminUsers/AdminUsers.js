import React from "react";
import { Box } from "@mui/material";

export default function AdminUsers() {
  return (
    <div>
      <Box
        component="main"
        sx={{ width: `calc(100% - ${165}px)`, ml: `${165}px` }}
      >
        <h3>Trang Quản lý người dùng</h3>
      </Box>
    </div>
  )
}
